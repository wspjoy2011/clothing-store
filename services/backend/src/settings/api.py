"""Paths the API is mounted on, shared by the application and the links it builds."""

API_VERSION_PREFIX = "/api/v1"

# Catalog
CATALOG_PRODUCTS_PATH = f"{API_VERSION_PREFIX}/catalog/products"
CATALOG_CATEGORIES_PATH = f"{API_VERSION_PREFIX}/catalog/categories"
